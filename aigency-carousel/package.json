{
  "name": "aigency-carousel",
  "private": true,
  "type": "module",
  "description": "The Aigency — Instagram carousel frames (1080×1350), built from the brand tokens.",
  "scripts": {
    "build": "node build.mjs",
    "render": "node build.mjs && node render.mjs"
  },
  "devDependencies": { "playwright": "1.56.1" }
}
