Capture the idea I describe as an intent.md.

Interview me like an analyst until it is concrete: the problem, who is
affected, what better looks like, constraints, what is out of scope, how we
will know it worked. Ask one question at a time. Stop when an engineer who
has never met me could act on it.

Then write `intent/<yyyy-mm-dd>-<slug>.md` using intent/TEMPLATE.md exactly.
Author: Abdullah Abudiak. Status: draft. Show me the file and wait for
corrections before committing. Commit message: `intent: <slug>`.

$ARGUMENTS
