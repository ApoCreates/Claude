Read the intent file at $ARGUMENTS and produce a requirements and design
spec for integrating it into this codebase.

Apply every skill available to you so the spec conforms to The Aigency
brand standards, the Supabase security rules and our UX conventions.
Read CLAUDE.md and the relevant source before writing.

Write `docs/sdlc/spec-<slug>.md` using docs/sdlc/spec-template.md.
Answer or carry forward every open question from the intent. Put a
"Flagged concerns" section first, above everything else, and be explicit
where two policies contradict each other. Do not write code.

Show me the spec and wait. On approval, commit as `spec: <slug>`.
